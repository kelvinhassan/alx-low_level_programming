#include "main.h"
/**
 * _abs - computer the absolute value of numbers from zero
 * @i: integer to be converted
 * Return: absolute value of input int
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = i * -1;
	}
	return (i);
}
