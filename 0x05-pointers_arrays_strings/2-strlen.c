#include "main.h"
/**
 * int _strlen - returns the length of a string
 * @s: pointer to the string to check
 * Return: void
 */
int _strlen(char *s)
{
	int l = 0;

	while (*s != '\0')
	{
		s++;
		i++;
	}
	return (1);
}
