#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: pointer to the string to check
 * Return: void
 */

int _strlen(char *s)
{
	int l = 0;

	int l = 0;
	{
		s++;
		l++;
	}
	return (l);
}
